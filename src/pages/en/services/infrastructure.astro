---
import PageLayout from '../../../layouts/PageLayout.astro';
import { getTranslation } from '../../../utils/i18n';

const lang = 'en';
const t = getTranslation(lang);
---

<PageLayout 
  title="Blockchain Infrastructure & Dev Tools - Aunova"
  description="Build robust blockchain and Web3 applications with our enterprise-grade blockchain infrastructure, development frameworks, testing suites, and deployment pipelines."
  lang={lang}
>
  <section class="service-hero">
    <div class="container">
      <div class="hero-content">
        <h1>Decentralized Infrastructure & Dev Tools</h1>
        <p class="lead">Enterprise-grade building blocks for the next generation of Web3 applications</p>
      </div>
    </div>
  </section>

  <section class="overview-section">
    <div class="container">
      <div class="overview-content">
        <h2>Foundation for Innovation</h2>
        <p>
          Building decentralized applications requires robust infrastructure and specialized development tools. We provide the essential components and frameworks that accelerate development while ensuring security, scalability, and reliability.
        </p>
        <p>
          From node infrastructure and RPC services to smart contract frameworks and monitoring solutions, our comprehensive toolkit enables teams to focus on innovation rather than infrastructure.
        </p>
      </div>
    </div>
  </section>

  <section class="features-section">
    <div class="container">
      <h2>Our Infrastructure Solutions</h2>
      <div class="features-grid">
        <div class="feature-card">
          <h3>Node Infrastructure</h3>
          <p>Deploy and manage blockchain nodes with enterprise-grade reliability and performance.</p>
          <ul>
            <li>Multi-chain support</li>
            <li>High availability clusters</li>
            <li>Load balancing</li>
            <li>Automatic failover</li>
            <li>Real-time monitoring</li>
          </ul>
        </div>
        
        <div class="feature-card">
          <h3>Development Frameworks</h3>
          <p>Accelerate development with our comprehensive smart contract and dApp frameworks.</p>
          <ul>
            <li>Modular architecture</li>
            <li>Security-first design</li>
            <li>Gas optimization</li>
            <li>Upgradeable patterns</li>
            <li>Testing integration</li>
          </ul>
        </div>
        
        <div class="feature-card">
          <h3>DevOps & Automation</h3>
          <p>Streamline deployment and operations with automated CI/CD pipelines and monitoring.</p>
          <ul>
            <li>Automated deployment</li>
            <li>Security scanning</li>
            <li>Performance monitoring</li>
            <li>Alert systems</li>
            <li>Custom dashboards</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <section class="use-cases-section">
    <div class="container">
      <h2>Real-World Applications</h2>
      <div class="use-cases-grid">
        <div class="use-case">
          <h3>DeFi Protocols</h3>
          <p>Build and deploy decentralized finance protocols with enterprise-grade infrastructure and security.</p>
        </div>
        <div class="use-case">
          <h3>NFT Platforms</h3>
          <p>Create scalable NFT marketplaces and platforms with optimized storage and metadata handling.</p>
        </div>
        <div class="use-case">
          <h3>DAOs</h3>
          <p>Deploy decentralized autonomous organizations with robust governance and treasury management.</p>
        </div>
        <div class="use-case">
          <h3>Cross-chain Bridges</h3>
          <p>Build secure and efficient bridges for asset transfer between different blockchain networks.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>Ready to Build on Solid Ground?</h2>
        <p>Let us provide the infrastructure and tools you need to build robust decentralized applications.</p>
        <div class="cta-actions">
          <a href={`/${lang}/contact`} class="btn btn-primary">Start Building</a>
          <a href="https://cal.com/ngmisl/aunova" target="_blank" rel="noopener" class="btn btn-outline">
            Schedule Consultation
          </a>
        </div>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  .service-hero {
    padding: var(--space-3xl) 0;
    background: linear-gradient(135deg, var(--color-background) 0%, color-mix(in srgb, var(--color-secondary) 8%, var(--color-background)) 100%);
  }
  
  .hero-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }
  
  .hero-content h1 {
    font-size: var(--text-4xl);
    font-weight: 700;
    margin-bottom: var(--space-md);
    /* Darker gradient for light mode for better readability */
    background: linear-gradient(135deg, var(--color-red-dark) 0%, #8B7F47 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1.3;
    padding-bottom: 0.1em;
  }
  
  /* Keep original gradient for dark mode */
  @media (prefers-color-scheme: dark) {
    .hero-content h1 {
      background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
  }
  
  .lead {
    font-size: var(--text-xl);
    color: var(--color-text-muted);
    line-height: 1.6;
  }
  
  .overview-section {
    padding: var(--space-3xl) 0;
  }
  
  .overview-content {
    max-width: 800px;
    margin: 0 auto;
  }
  
  .overview-content h2 {
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-lg);
    text-align: center;
  }
  
  .overview-content p {
    font-size: var(--text-lg);
    line-height: 1.8;
    color: var(--color-text);
    margin-bottom: var(--space-lg);
  }
  
  .features-section {
    padding: var(--space-3xl) 0;
    background-color: color-mix(in srgb, var(--color-secondary) 5%, var(--color-background));
  }
  
  .features-section h2 {
    text-align: center;
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-2xl);
  }
  
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-xl);
  }
  
  .feature-card {
    background-color: var(--color-background);
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border);
    transition: all var(--transition-base);
  }
  
  .feature-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-lg);
    transform: translateY(-2px);
  }
  
  .feature-card h3 {
    font-size: var(--text-xl);
    font-weight: 700;
    margin-bottom: var(--space-md);
    color: var(--color-primary);
  }
  
  .feature-card p {
    color: var(--color-text-muted);
    line-height: 1.6;
    margin-bottom: var(--space-md);
  }
  
  .feature-card ul {
    list-style: none;
    padding: 0;
  }
  
  .feature-card li {
    padding-left: var(--space-lg);
    position: relative;
    margin-bottom: var(--space-sm);
    color: var(--color-text);
  }
  
  .feature-card li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--color-secondary);
    font-weight: bold;
  }
  
  .use-cases-section {
    padding: var(--space-3xl) 0;
  }
  
  .use-cases-section h2 {
    text-align: center;
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-2xl);
  }
  
  .use-cases-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-xl);
  }
  
  .use-case {
    text-align: center;
    padding: var(--space-lg);
  }
  
  .use-case h3 {
    font-size: var(--text-lg);
    font-weight: 700;
    margin-bottom: var(--space-sm);
    color: var(--color-primary);
  }
  
  .use-case p {
    color: var(--color-text-muted);
    line-height: 1.6;
  }
  
  .cta-section {
    padding: var(--space-3xl) 0;
    background: linear-gradient(135deg, color-mix(in srgb, var(--color-primary) 5%, var(--color-background)) 0%, var(--color-background) 100%);
  }
  
  .cta-content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }
  
  .cta-content h2 {
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-md);
  }
  
  .cta-content p {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    margin-bottom: var(--space-2xl);
  }
  
  .cta-actions {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-sm) var(--space-xl);
    border-radius: var(--radius-md);
    font-weight: 600;
    font-size: var(--text-base);
    transition: all var(--transition-fast);
    text-decoration: none;
    cursor: pointer;
    border: 2px solid transparent;
  }
  
  .btn-primary {
    background-color: var(--color-primary);
    color: var(--color-white);
    border-color: var(--color-primary);
  }
  
  .btn-primary:hover {
    background-color: color-mix(in srgb, var(--color-primary) 85%, black);
    border-color: color-mix(in srgb, var(--color-primary) 85%, black);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  .btn-outline {
    background-color: transparent;
    color: var(--color-primary);
    border-color: var(--color-primary);
  }
  
  .btn-outline:hover {
    background-color: var(--color-primary);
    color: var(--color-white);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  @media (max-width: 768px) {
    .hero-content h1 {
      font-size: var(--text-3xl);
    }
    
    .features-grid,
    .use-cases-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
