---
import PageLayout from '../../layouts/PageLayout.astro';
import { getTranslation } from '../../utils/i18n';

const lang = 'es';
const t = getTranslation(lang);
---

<PageLayout 
  title="Contacta con Aunova - Agenda una Consulta"
  description="Ponte en contacto con Aunova para soluciones de Zero-Knowledge e IA. Agenda una consulta para discutir las necesidades de tu proyecto."
  lang={lang}
>
  <section class="contact-hero">
    <div class="container">
      <div class="hero-content">
        <h1>{t.contact.title}</h1>
        <p>{t.contact.subtitle}</p>
      </div>
    </div>
  </section>

  <section class="contact-section">
    <div class="container">
      <div class="contact-options">
        <!-- Calendar Integration -->
        <div class="contact-card">
          <div class="card-content">
            <div class="contact-icon">
              <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                <line x1="16" y1="2" x2="16" y2="6"></line>
                <line x1="8" y1="2" x2="8" y2="6"></line>
                <line x1="3" y1="10" x2="21" y2="10"></line>
              </svg>
            </div>
            <h2>{t.contact.calendar.title}</h2>
            <p>{t.contact.calendar.description}</p>
          </div>
          <a 
            href="https://cal.com/ngmisl/aunova" 
            target="_blank" 
            rel="noopener"
            class="btn btn-primary"
          >
            {t.contact.calendar.button}
          </a>
        </div>
        
        <!-- Email -->
        <div class="contact-card">
          <div class="card-content">
            <div class="contact-icon">
              <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                <polyline points="22,6 12,13 2,6"></polyline>
              </svg>
            </div>
            <h2>Envíanos un Email</h2>
            <p>Envíanos un mensaje directamente por correo electrónico</p>
          </div>
          <a href="mailto:chris@aunova.net" class="btn btn-secondary">
            Contactar
          </a>
        </div>
        
        <!-- Bluesky -->
        <div class="contact-card">
          <div class="card-content">
            <div class="contact-icon">
              <svg width="48" height="48" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 2L2 7v10c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V7l-10-5z"/>
            </svg>
            </div>
            <h2>Síguenos en Bluesky</h2>
            <p>Conéctate con nosotros en las redes sociales descentralizadas</p>
          </div>
          <a href="https://bsky.app/profile/aunova.net" target="_blank" rel="noopener" class="btn btn-outline">
            @aunova.net
          </a>
        </div>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  .contact-hero {
    padding: var(--space-3xl) 0;
    background: linear-gradient(135deg, var(--color-background) 0%, color-mix(in srgb, var(--color-secondary) 10%, var(--color-background)) 100%);
  }
  
  .hero-content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }
  
  .hero-content h1 {
    font-size: var(--text-4xl);
    font-weight: 700;
    margin-bottom: var(--space-md);
  }
  
  .hero-content p {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
  }
  
  .contact-section {
    padding: var(--space-3xl) 0 var(--space-5xl);
  }
  
  .contact-options {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-xl);
    max-width: 1000px;
    margin: 0 auto;
  }
  
  .contact-card {
    text-align: center;
    padding: var(--space-2xl);
    background-color: var(--color-background);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    transition: all var(--transition-base);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: 320px;
  }
  
  .card-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    flex-grow: 1;
  }
  
  .contact-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-lg);
    transform: translateY(-2px);
  }
  
  .contact-icon {
    display: inline-flex;
    justify-content: center;
    align-items: center;
    width: 80px;
    height: 80px;
    margin: 0 auto var(--space-lg);
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    border-radius: var(--radius-full);
    color: var(--color-white);
  }
  
  .contact-card h2 {
    font-size: var(--text-xl);
    font-weight: 700;
    margin-bottom: var(--space-md);
  }
  
  .contact-card p {
    color: var(--color-text-muted);
    margin-bottom: 0;
    line-height: 1.6;
    flex-grow: 1;
  }
  
  /* Buttons */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-sm) var(--space-xl);
    border-radius: var(--radius-md);
    font-weight: 600;
    font-size: var(--text-base);
    transition: all var(--transition-fast);
    text-decoration: none;
    cursor: pointer;
    border: 2px solid transparent;
  }
  
  .btn-primary {
    background-color: var(--color-primary);
    color: var(--color-white);
    border-color: var(--color-primary);
  }
  
  .btn-primary:hover {
    background-color: color-mix(in srgb, var(--color-primary) 85%, black);
    border-color: color-mix(in srgb, var(--color-primary) 85%, black);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  .btn-secondary {
    background-color: var(--color-secondary);
    color: var(--color-black);
    border-color: var(--color-secondary);
  }
  
  .btn-secondary:hover {
    background-color: color-mix(in srgb, var(--color-secondary) 85%, white);
    border-color: color-mix(in srgb, var(--color-secondary) 85%, white);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  .btn-outline {
    background-color: transparent;
    color: var(--color-primary);
    border-color: var(--color-primary);
  }
  
  .btn-outline:hover {
    background-color: var(--color-primary);
    color: var(--color-white);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .contact-options {
      grid-template-columns: 1fr;
    }
    
    .hero-content h1 {
      font-size: var(--text-3xl);
    }
  }
</style>
